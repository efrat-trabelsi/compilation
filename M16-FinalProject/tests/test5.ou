/* Mixed types and casting */
i: int;
f: float;
result: float;
{
    i = 10;
    f = 3.5;
    result = cast<float>(i) + f;
    output(result);
    i = cast<int>(result);
    output(i);
}

/* 
Expected translation to QUAD:
IASN i 10
RASN f 3.5
ITOR t1 i
RADD result t1 f
RPRT result
RTOI i result
IPRT i
HALT
*/
